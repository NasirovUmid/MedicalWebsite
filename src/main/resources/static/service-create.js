const serviceForm = document.querySelector('#service-form'); const serviceMessage = document.querySelector('#form-message');
serviceForm.addEventListener('submit', async event => { event.preventDefault(); const data = Object.fromEntries(new FormData(serviceForm)); try { const response = await fetch('/services', { method:'POST', headers:{ 'Content-Type':'application/json', ...authHeaders }, body:JSON.stringify({ ...data, price:Number(data.price), durance:Number(data.durance) }) }); if (!response.ok) throw new Error(); serviceMessage.textContent = 'Услуга создана.'; serviceMessage.className = 'message success'; } catch { serviceMessage.textContent = 'Не удалось создать услугу.'; serviceMessage.className = 'message error'; } });
