const form = document.querySelector('#form043'); const message = document.querySelector('#form-message'); const patientId = new URLSearchParams(location.search).get('userId');
if (!patientId) location.href = 'admin.html';
form.addEventListener('submit', async event => { event.preventDefault(); const values = Object.fromEntries(new FormData(form)); const now = new Date().toISOString(); const body = { ...values, id: crypto.randomUUID(), userId: patientId, birthDate: new Date(values.birthDate).toISOString(), observationStartDate: now, observationEndDate: now, date: now }; try { const response = await fetch('/form043', { method: 'POST', headers: { 'Content-Type':'application/json', ...authHeaders }, body: JSON.stringify(body) }); if (!response.ok) throw new Error(); message.textContent = 'Форма 043 создана.'; message.className = 'message success'; setTimeout(() => location.href = 'admin.html', 700); } catch { message.textContent = 'Не удалось сохранить форму. Проверьте заполнение полей.'; message.className = 'message error'; } });
